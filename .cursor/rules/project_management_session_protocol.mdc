---
description: Protocole gestion de projet (lecture docs + tache + journal)
alwaysApply: true
---

# Regle: gestion de projet (session)

Quand la demande utilisateur concerne la gestion de projet (ex: "tache du jour", "prochaine tache", "priorite", "prochaine session", "ou en est-on", "quoi faire"), appliquer ce protocole avant de repondre.

## 1) Ouverture docs obligatoire (ordre de lecture)

Lire systematiquement:
- `WORKFLOW_PROTOCOL.md`
- `ASSISTANT_CONTEXT.md`
- `PROJECT_LOG.md` (derniere entree datee)
- `Notes/Todo_project.md` (section "Prochaine session" / "priorite immediate")

Objectif: retrouver la priorite reelle de la session en cours, sans invention.

Verifier aussi la branche Git (`git branch --show-current` ou `git status -sb`) et le bloc **Contexte Git** dans `Notes/Todo_project.md`.

**Gate pull (auteur) :** par **session** (tampon `.cursor/session_pull_ok`), pas par chat, pas « un pull max par jour ». Nouvelle session (journal ferme, autre machine, « on reprend » / tâche du jour) → coller `powershell -ExecutionPolicy Bypass -File .\scripts\session-git-sync.ps1` + credits Bezy. **Aucun prompt** tant que « pull ok » (`.cursor/rules/session_git_sync.mdc`).

## 2) Regle de reponse (anti-bruit)

- Ne jamais inventer une "tache du jour".
- **Si la branche courante n'est pas `main` :** commencer la reponse par un rappel explicite du type *« Tu es sur la branche feature `…` — le travail en cours est … »* (nom de branche = sortie Git ou bloc *Contexte Git* du todo).
- **Credits Bezy :** rappeler que le reset mensuel est le **30 de chaque mois** (pas le 1er, pas le dernier jour si ≠ 30) — sources `ASSISTANT_CONTEXT.md`, `Notes/Bezi/README_bezi.md`.
- **Pull Git (ouverture de session, pas chaque chat) :** lire `.cursor/session_pull_ok`. Skip seulement si `open` **et** meme session (pas les mots-cles nouvelle session). Plusieurs sessions le meme jour = nouveau pull. Regle : `.cursor/rules/session_git_sync.mdc`.
- **Skill Bezy (prod) :** rappeler `Notes/Bezi/WORKFLOW_skill_prefab_ui.md` et proposer un bloc `/prefab-ui-3phases` pret (Task ID + prefab + phase) **apres** confirmation du pull. Peu de temps auteur ≠ skip Bezy.
- Repondre avec la priorite la plus recente, concrete et actionnable, basee sur les documents.
- Si plusieurs priorites existent, proposer un ordre clair (1, 2, 3) avec la priorite immediate en premier.
- Si les sources se contredisent, le dire explicitement et demander validation.

## 3) Enregistrement de tache (memoire de session)

Si l'utilisateur donne/modifie une tache de prochaine session:
1. mettre a jour `Notes/Todo_project.md`,
2. ajouter la trace dans la nouvelle entree de `PROJECT_LOG.md`,
3. confirmer a l'utilisateur que cette tache sera la reference lors de la prochaine demande "tache du jour".

## 4) Fin de session

Quand l'utilisateur indique la fin de session ou demande de journaliser:
- proposer "Mets a jour le journal" si necessaire,
- enregistrer objectifs, changements, decisions, problemes/solutions, prochaines etapes,
- passer `.cursor/session_pull_ok` a `status: closed` (prochain chat = nouveau gate pull),
- **ne pas committer** les docs : rappeler a l'auteur de `git add` / `git commit` / `git push` (cf. `.cursor/rules/git_commits_user_only.mdc`).

## 5) Priorite courante connue (memoire active)

Branche courante documentee: **`feature/biofiltre-isometric`** (2026-09-05, depuis `main`).
- Workflow Bezy prod : `Notes/Bezi/WORKFLOW_skill_prefab_ui.md` (`/prefab-ui-3phases`).
- Branche brouillon Workspace Rules **supprimee** : `cursor/bezi-workspace-rules-skill-76a4`.

Priorite immediate actuelle:
- `[P0-FARM-ISO-GRID-001]` sur `feature/biofiltre-isometric` : geometrie losange 2:1 en place ; art IBC losange en attente auteur.
- Bezy `[P0-TAB-SPRITES-001]` / `[BZ-TAB-SPRITES-001]` : mise en place + polish sprites onglets HUD. Prompts apres brief visuel auteur + validation (`Notes/Ui/PROMPTS_Bezi_tab_sprites.md`).

Reportes: [P0-FARM-SPRITE-ALPHA-001] fond noir laitue, [P0-SALE-QTY-RAND-001] rand 1-3 salades ★1.

Clos: [P0-FARM-IBC-GRID-001] cuve IBC + grille carree — playtest OK 2026-09-02.
Clos: [P0-SALE-STAR-PLAY-001] playtest tooltip etoiles 3 bandeaux vente — OK 2026-08-30.
Clos: [P0-FARM-GRID-PLAY-001] grille sans colliders + clics + pose/recolte + pause/recall — OK 2026-08-30 (feature/rework-biofiltre-grid).
Clos: [P0-FARM-PLANT-TOUCH-001] pose tactile — OK 2026-08-30 (meme playtest).
